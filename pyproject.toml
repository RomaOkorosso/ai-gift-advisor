[project]
name = "ai-gift-advisor"
version = "0.1.0"
description = "AI-powered gift recommendation service"
requires-python = ">=3.14"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]
